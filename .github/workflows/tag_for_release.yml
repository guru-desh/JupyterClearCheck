name: Bump version and Create PR

on:
  workflow_dispatch:
    inputs:
      bump-type:
        description: "Bump type"
        required: true
        default: "patch"
        type: choice
        options:
          - major
          - minor
          - patch

jobs:
  bump-version:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up PDM
        uses: pdm-project/setup-pdm@v4
        with:
          python-version: "3.9"

      - name: Install dependencies
        run: pdm install

      - name: Configure Git
        run: |
          git config --local user.name "GitHub Actions"
          git config --local user.email "actions@github.com"

      - name: Bump version and commit changes
        run: |
          pdm run bump-version ${{ github.event.inputs['bump-type'] }} --commit

      - name: Capture new version
        run: |
          VERSION=$(pdm show --version)
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Create and checkout release branch
        run: |
          git checkout -b release/v${{ env.VERSION }}

      - name: Push changes to release branch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git push --set-upstream origin release/v${{ env.VERSION }}

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "Bump version to ${{ env.VERSION }}"
          branch: "release/v${{ env.VERSION }}"
          base: main
          title: "Release v${{ env.VERSION }}"
          body: "This PR contains the release version bump to ${{ env.VERSION }}."
          labels: "automated PR"
